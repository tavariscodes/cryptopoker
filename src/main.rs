use pokersite::poker::deck::*;
use pokersite::poker::table::*;
use pokersite::poker::player::*;

fn main() {
    let mut testvec = vec![1,3,3,2,6,6,5];
    let sortedvec = testvec.sort();
    return sortedvec
    // let mut test_deck = Deck::new();
    // assert_eq!(52, test_deck.cards.len());

    // let test_suit = Suit::Club;
    // assert_eq!('c', test_suit.to_char());

    // test_deck.shuffle();
    // let table_id = String::from("727-111");
    // let mut test_table = Table::new(table_id, 0.10, 0.20, 5.00, 20.00, 6);
    // let mut test_player = Player::new(String::from("Tavaris"), 60.0, true);

    // test_table.player_joined(test_player);
    // test_table.start_round();


    
}
